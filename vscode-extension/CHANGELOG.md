# Changelog

## [0.1.3]

- Fixed: binary default now uses `sdd` instead of `gsd`
- Fixed: sidebar, chat, and all commands fully rebranded to SDD
- Fixed: output channel renamed to "SDD Agent"
- Updated: SDD logo for extension icon

## [0.1.2]

- Fixed: README updated with all SDD references (zero GSD remaining)
- Updated: publisher set to `bhargavvc`

## [0.1.1]

- Added: SDD logo (replaced old GSD logo)
- Updated: README rebranded to SDD

## [0.1.0]

Initial release — forked from GSD-2 VS Code extension.

- Full RPC client — spawns `sdd --mode rpc`, JSON line framing, all RPC commands
- Sidebar dashboard — connection status, model info, thinking level, token usage, cost, quick actions
- Chat participant — `@sdd` in VS Code Chat with streaming responses
- 15 commands with keyboard shortcuts
- Auto-start and auto-compaction configuration
